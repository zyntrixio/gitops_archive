---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: snowstorm
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${snowstorm_client_id}
  labels:
    azure.workload.identity/use: "true"
