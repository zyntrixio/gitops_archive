---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: snowstorm
  namespace: default
  labels:
    azure.workload.identity/use: "true"
    azure.workload.identity/client-id: ${snowstorm_client_id}
