apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../bases/deployment

nameSuffix: -event-processor-hermes

commonLabels:
  component: event-processor
  queue: clickhouse-hermes

patches:
  - target:
      kind: Deployment
    patch: |
      - op: add
        path: /spec/template/spec/containers/0/args
        value: ["stormctl", "deploy", "events", "--queues=clickhouse_hermes"]
      - op: add
        path: /spec/template/spec/priorityClassName
        value: bink-high-tasks
