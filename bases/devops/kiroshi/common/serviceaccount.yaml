---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kiroshi
  namespace: devops
  annotations:
    azure.workload.identity/client-id: ${kiroshi_client_id}
  labels:
    azure.workload.identity/use: "true"
