apiVersion: apps/v1
kind: Deployment
metadata:
  name: locust
spec:
  template:
    spec:
      containers:
      - name: app
        command:
        - locust
        - --worker
        - --master-host=locust-master
        - --master-port=5557
        - --locustfile=locustfile_peak_second.py
