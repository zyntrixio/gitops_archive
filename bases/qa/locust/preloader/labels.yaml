apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: locust
    role: preloader
  name: locust
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
