apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: locust-preloader
parameters:
  kind: managed
  location: uksouth
  skuName: Standard_LRS
allowVolumeExpansion: true
provisioner: kubernetes.io/azure-disk
reclaimPolicy: Delete
volumeBindingMode: Immediate
