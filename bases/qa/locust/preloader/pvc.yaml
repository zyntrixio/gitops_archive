apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: locust-preloader
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 512Gi
  storageClassName: locust-preloader
