apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: hermes
  namespace: prometheus
  labels:
    name: hermes
spec:
  selector:
    matchLabels:
      app: hermes
  namespaceSelector:
    matchNames:
      - olympus
  endpoints:
    - targetPort: 9100
