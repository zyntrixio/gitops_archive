apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cadvisor
  labels:
    name: cadvisor
spec:
  namespaceSelector:
    matchNames:
      - cadvisor
  selector:
    matchLabels:
      app: cadvisor
  podMetricsEndpoints:
  - targetPort: 8080
