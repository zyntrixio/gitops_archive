---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: prefect
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${prefect_client_id}
  labels:
    azure.workload.identity/use: "true"
