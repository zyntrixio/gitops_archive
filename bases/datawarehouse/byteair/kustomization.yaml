resources:
  - namespace.yaml
  - helm-release.yaml
  - helm-repo.yaml
  - serviceaccount.yaml

configMapGenerator:
  - name: airbyte-values
    namespace: airbyte
    files:
      - values.yaml

configurations:
  - kustomizeconfig.yaml
