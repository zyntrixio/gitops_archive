apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: airbyte
  namespace: airbyte
spec:
  interval: 5m
  valuesFrom:
  - kind: ConfigMap
    name: airbyte-values
  chart:
    spec:
      chart: airbyte
      version: 0.50.13
      sourceRef:
        kind: HelmRepository
        name: airbyte
        namespace: flux-system
      interval: 1m
