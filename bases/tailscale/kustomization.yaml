apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://github.com/tailscale/tailscale/blob/49fd0a62c9bd24eaed7e612d1eeb3ca4b89575fe/cmd/k8s-operator/deploy/manifests/operator.yaml?raw=true
namespace: tailscale
secretGenerator:
  - name: operator-oauth
    namespace: tailscale
    behavior: replace
    literals:
      - client_id=kNvPeQ2CNTRL
      - client_secret=tskey-client-kNvPeQ2CNTRL-L9R2f3DqnWiJskqdn2DeWiaZjEc9a9iWh
patches:
  - target:
      kind: Deployment
      name: operator
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: operator
      spec:
        template:
          spec:
            containers:
            - name: operator
              env:
              - name: OPERATOR_HOSTNAME
                value: ${cluster_location}-${cluster_name}
              - name: PROXY_TAGS
                value: tag:${cluster_name}
