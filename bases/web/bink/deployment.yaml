---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: web-bink
  name: web-bink
  namespace: default
spec:
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  selector:
    matchLabels:
      app: web-bink
  template:
    metadata:
      labels:
        app: web-bink
      annotations:
        linkerd.io/inject: enabled
        kubectl.kubernetes.io/default-container: app
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - web-bink
                topologyKey: topology.kubernetes.io/zone
              weight: 100
      containers:
        - name: app
          image: binkcore.azurecr.io/web-bink:latest
          imagePullPolicy: Always
      imagePullSecrets:
        - name: binkcore.azurecr.io
