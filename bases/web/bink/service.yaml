---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-bink
  name: web-bink
  namespace: default
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: web-bink
  sessionAffinity: None
  type: ClusterIP
