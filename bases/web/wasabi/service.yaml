---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-wasabi
  name: web-wasabi
  namespace: default
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: web-wasabi
  sessionAffinity: None
  type: ClusterIP
