apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: europa
    component: api
  name: europa
  namespace: default
spec:
  selector:
    matchLabels:
      app: europa
      component: api
  template:
    metadata:
      labels:
        app: europa
        component: api
        aadpodidbinding: europa
