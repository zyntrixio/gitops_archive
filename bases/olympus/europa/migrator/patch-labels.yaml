apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: europa
    component: migrator
  name: europa
  namespace: default
spec:
  selector:
    matchLabels:
      app: europa
      component: migrator
  template:
    metadata:
      labels:
        app: europa
        component: migrator
