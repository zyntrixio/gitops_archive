apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: europa
    component: api
  name: europa
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
