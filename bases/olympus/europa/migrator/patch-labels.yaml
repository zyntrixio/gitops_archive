apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: europa
    component: migrator
  name: europa
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
