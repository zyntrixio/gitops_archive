---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: europa
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${europa_client_id}
  labels:
    azure.workload.identity/use: "true"
