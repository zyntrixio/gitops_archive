apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: midas
    component: consumer
  name: midas
  namespace: default
spec:
  selector:
    matchLabels:
      app: midas
      component: consumer
  template:
    metadata:
      labels:
        app: midas
        component: consumer
        aadpodidbinding: midas
