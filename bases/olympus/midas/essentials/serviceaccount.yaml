---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: midas
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${midas_client_id}
  labels:
    azure.workload.identity/use: "true"
