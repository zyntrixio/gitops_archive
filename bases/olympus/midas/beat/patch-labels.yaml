apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: midas
    component: beat
  name: midas
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
