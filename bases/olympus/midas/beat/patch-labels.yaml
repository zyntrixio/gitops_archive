apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: midas
    component: beat
  name: midas
  namespace: default
spec:
  selector:
    matchLabels:
      app: midas
      component: beat
  template:
    metadata:
      labels:
        app: midas
        component: beat
