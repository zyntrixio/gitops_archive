apiVersion: apps/v1
kind: Deployment
metadata:
  name: midas
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        command:
        - celery
        - -A
        - app
        - beat
        - --schedule
        - /tmp/beat
        - --pidfile
        - /tmp/beat.pid
