---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: midas
    component: worker
  name: midas
  namespace: default
spec:
  selector:
    matchLabels:
      app: midas
      component: worker
  template:
    metadata:
      labels:
        app: midas
        component: worker
        aadpodidbinding: midas
