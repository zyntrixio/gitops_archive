apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: midas
    component: celery
  name: midas
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
