apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: midas
    component: celery
  name: midas
  namespace: default
spec:
  selector:
    matchLabels:
      app: midas
      component: celery
  template:
    metadata:
      labels:
        app: midas
        component: celery
        aadpodidbinding: midas
