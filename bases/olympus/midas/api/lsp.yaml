---
apiVersion: linkerd.io/v1alpha2
kind: ServiceProfile
metadata:
  name: midas-api.default.svc.cluster.local
  namespace: default
spec:
  routes:
    - condition:
        method: GET
        pathRegex: /healthz
      name: GET /healthz
      isRetryable: true
      timeout: 100ms
