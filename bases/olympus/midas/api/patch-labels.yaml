apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: midas
    component: api
  name: midas
  namespace: default
spec:
  selector:
    matchLabels:
      app: midas
      component: api
  template:
    metadata:
      labels:
        app: midas
        component: api
