apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: midas
    component: api
    aadpodidbinding: midas
  name: midas
  namespace: default
spec:
  selector:
    matchLabels:
      app: midas
      component: api
      aadpodidbinding: midas
  template:
    metadata:
      labels:
        app: midas
        component: api
        aadpodidbinding: midas
