apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: zagreus
    component: consumer
  name: zagreus
  namespace: default
spec:
  selector:
    matchLabels:
      app: zagreus
      component: consumer
  template:
    metadata:
      labels:
        app: zagreus
        component: consumer
