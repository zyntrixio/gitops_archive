apiVersion: apps/v1
kind: Deployment
metadata:
  name: zagreus
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        command:
        - python
        - manage.py
        - consume
