apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: zagreus
    component: api
  name: zagreus
  namespace: default
spec:
  selector:
    matchLabels:
      app: zagreus
      component: api
  template:
    metadata:
      labels:
        app: zagreus
        component: api
