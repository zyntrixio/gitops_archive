apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: zagreus
    component: migrator
  name: zagreus
  namespace: default
spec:
  selector:
    matchLabels:
      app: zagreus
      component: migrator
  template:
    metadata:
      labels:
        app: zagreus
        component: migrator
