apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: pelops
    component: api
  name: pelops
  namespace: default
spec:
  selector:
    matchLabels:
      app: pelops
      component: api
  template:
    metadata:
      labels:
        app: pelops
        component: api
