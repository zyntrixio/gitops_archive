apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: pelops
    component: api
  name: pelops
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
