apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: metis
    component: celery
  name: metis
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
