apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: metis
    component: celery
  name: metis
  namespace: default
spec:
  selector:
    matchLabels:
      app: metis
      component: celery
  template:
    metadata:
      labels:
        app: metis
        component: celery
