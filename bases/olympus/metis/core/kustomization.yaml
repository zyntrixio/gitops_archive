resources:
  - deployment.yaml

components:
  - ../../../common/pushgateway
  - ../../../common/pgbouncer

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /spec/template/metadata/labels
        value:
          aadpodidbinding: metis
