apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: metis
    component: api
  name: metis
  namespace: default
spec:
  selector:
    matchLabels:
      app: metis
      component: api
  template:
    metadata:
      labels:
        app: metis
        component: api
