apiVersion: apps/v1
kind: Deployment
metadata:
  name: metis
  namespace: default
spec:
  template:
    metadata:
      annotations:
        prometheus.io/port: '9100'
        prometheus.io/scrape: 'true'
        prometheus.io/job: metis
