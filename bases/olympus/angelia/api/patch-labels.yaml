apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: angelia
    component: api
  name: angelia
  namespace: default
spec:
  selector:
    matchLabels:
      app: angelia
      component: api
  template:
    metadata:
      labels:
        app: angelia
        component: api
