---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: angelia
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${angelia_client_id}
  labels:
    azure.workload.identity/use: "true"
