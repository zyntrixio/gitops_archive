apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: skiron
    component: api
  name: skiron
  namespace: default
spec:
  selector:
    matchLabels:
      app: skiron
      component: api
  template:
    metadata:
      labels:
        app: skiron
        component: api
