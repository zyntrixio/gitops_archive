apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: skiron
    component: api
  name: skiron
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
