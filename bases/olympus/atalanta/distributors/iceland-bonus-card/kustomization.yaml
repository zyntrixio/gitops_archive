bases:
  - "../../core/job"

commonLabels:
  component: distributor-iceland-bonus-card

nameSuffix: -distributor-iceland-bonus-card

patches:
  - target:
      kind: CronJob
    patch: |-
      - op: replace
        path: /spec/jobTemplate/spec/template/spec/containers/0/args
        value: [ cargo, run, --bin, distributor ]
