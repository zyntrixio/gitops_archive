bases:
  - "../../core/deployment"

commonLabels:
  component: distributor-costa

nameSuffix: -distributor-costa


patches:
  - target:
      kind: Deployment
    patch: |-
      - op: replace
        path: /spec/jobTemplate/spec/template/spec/containers/0/args
        value: [ cargo, run, --bin, distributor ]