bases:
  - "../../core/deployment"

commonLabels:
  component: transactor-iceland-bonus-card

nameSuffix: -transactor-iceland-bonus-card

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: replace
        path: /spec/jobTemplate/spec/template/spec/containers/0/args
        value: [ cargo, run, --bin, transactor ]
