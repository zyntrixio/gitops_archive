bases:
  - "../../core/deployment"

commonLabels:
  component: transactor-wasabi-club

nameSuffix: -transactor-wasabi-club

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: replace
        path: /spec/jobTemplate/spec/template/spec/containers/0/args
        value: [ cargo, run, --bin, transactor ]
