apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: zephyrus
    component: api
  name: zephyrus
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
