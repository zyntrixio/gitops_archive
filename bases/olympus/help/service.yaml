---
apiVersion: v1
kind: Service
metadata:
  name: help
  namespace: default
  labels:
    app: help
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: help
  sessionAffinity: None
  type: ClusterIP
