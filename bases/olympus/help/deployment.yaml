---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: help
  name: help
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: help
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
        kubectl.kubernetes.io/default-container: app
      labels:
        app: help
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - help
                topologyKey: topology.kubernetes.io/zone
              weight: 100
      containers:
        - name: app
          image: binkcore.azurecr.io/help.gb.bink.com:staging
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            limits:
              memory: 150Mi
              cpu: 1000m
            requests:
              cpu: 10m
              memory: 150Mi
      imagePullSecrets:
        - name: binkcore.azurecr.io
