apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hermes
    component: celery
  name: hermes
  namespace: default
spec:
  selector:
    matchLabels:
      app: hermes
      component: celery
  template:
    metadata:
      labels:
        app: hermes
        component: celery
