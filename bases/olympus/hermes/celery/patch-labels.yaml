apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: hermes
    component: celery
  name: hermes
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
