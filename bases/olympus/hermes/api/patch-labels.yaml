apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: hermes
    component: api
  name: hermes
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
