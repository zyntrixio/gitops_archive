apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hermes
    component: api
  name: hermes
  namespace: default
spec:
  selector:
    matchLabels:
      app: hermes
      component: api
  template:
    metadata:
      labels:
        app: hermes
        component: api
