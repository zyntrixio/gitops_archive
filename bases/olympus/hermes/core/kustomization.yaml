resources:
- deployment.yaml

components:
- ../../../common/pgbouncer
- ../../../common/pushgateway

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /spec/template/metadata/labels
        value:
          aadpodidbinding: hermes
