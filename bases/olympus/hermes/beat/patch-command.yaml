apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        command:
        - celery
        - -A
        - hermes
        - beat
        - --schedule
        - /tmp/beat
        - --pidfile
        - /tmp/beat.pid
