apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hermes
    component: beat
  name: hermes
  namespace: default
spec:
  selector:
    matchLabels:
      app: hermes
      component: beat
  template:
    metadata:
      labels:
        app: hermes
        component: beat
