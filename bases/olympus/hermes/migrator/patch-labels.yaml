apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hermes
    component: migrator
  name: hermes
  namespace: default
spec:
  selector:
    matchLabels:
      app: hermes
      component: migrator
  template:
    metadata:
      labels:
        app: hermes
        component: migrator
