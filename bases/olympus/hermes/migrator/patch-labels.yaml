apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: hermes
    component: migrator
  name: hermes
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
