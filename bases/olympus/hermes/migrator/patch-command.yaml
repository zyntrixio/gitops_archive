apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        command:
        - bash
        - -c
        - sleep 10; python manage.py migrate; echo Done; tail -f /dev/null
