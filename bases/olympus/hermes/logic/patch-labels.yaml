apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hermes
    component: logic
  name: hermes
  namespace: default
spec:
  selector:
    matchLabels:
      app: hermes
      component: logic
  template:
    metadata:
      labels:
        app: hermes
        component: logic
