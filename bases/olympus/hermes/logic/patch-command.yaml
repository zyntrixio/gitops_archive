apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        command:
        - python
        - api_messaging/run.py
