apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hades
    component: migrator
  name: hades
  namespace: default
spec:
  selector:
    matchLabels:
      app: hades
      component: migrator
  template:
    metadata:
      labels:
        app: hades
        component: migrator
