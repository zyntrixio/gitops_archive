---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hades
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${hades_client_id}
  labels:
    azure.workload.identity/use: "true"
