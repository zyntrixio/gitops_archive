apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: hades
    component: api
  name: hades
  namespace: default
spec:
  selector:
    matchLabels:
      app: hades
      component: api
  template:
    metadata:
      labels:
        app: hades
        component: api
