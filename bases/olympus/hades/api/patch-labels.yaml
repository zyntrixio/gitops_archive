apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: hades
    component: api
  name: hades
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
