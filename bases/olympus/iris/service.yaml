apiVersion: v1
kind: Service
metadata:
  labels:
    app: iris
    component: api
  name: iris-api
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    app: iris
    component: api
  sessionAffinity: None
  type: ClusterIP
