apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: atlas
    component: migrator
  name: atlas
  namespace: default
spec:
  selector:
    matchLabels:
      app: atlas
      component: migrator
  template:
    metadata:
      labels:
        app: atlas
        component: migrator
