apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: atlas
    component: consumer
  name: atlas
  namespace: default
spec:
  selector:
    matchLabels:
      app: atlas
      component: consumer
  template:
    metadata:
      labels:
        app: atlas
        component: consumer
