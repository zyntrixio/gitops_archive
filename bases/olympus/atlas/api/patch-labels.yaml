apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: atlas
    component: api
  name: atlas
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
