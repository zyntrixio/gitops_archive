apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: atlas
    component: api
  name: atlas
  namespace: default
spec:
  selector:
    matchLabels:
      app: atlas
      component: api
  template:
    metadata:
      labels:
        app: atlas
        component: api
