apiVersion: v1
kind: Service
metadata:
  labels:
    app: atlas
    component: api
  name: atlas-api
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    app: atlas
    component: api
  sessionAffinity: None
  type: ClusterIP
