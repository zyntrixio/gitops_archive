---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: harmonia
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${harmonia_client_id}
  labels:
    azure.workload.identity/use: "true"
