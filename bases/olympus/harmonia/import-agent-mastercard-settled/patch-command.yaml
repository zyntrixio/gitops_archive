apiVersion: apps/v1
kind: Deployment
metadata:
  name: harmonia
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        args:
        - --agent
        - mastercard-settled
        - --no-user-input
        - --quiet
        command:
        - tximport
