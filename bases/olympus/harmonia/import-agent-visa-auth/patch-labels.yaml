apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: harmonia
    component: import-agent-visa-auth
  name: harmonia
  namespace: default
spec:
  selector:
    matchLabels:
      app: harmonia
      component: import-agent-visa-auth
  template:
    metadata:
      labels:
        app: harmonia
        component: import-agent-visa-auth
