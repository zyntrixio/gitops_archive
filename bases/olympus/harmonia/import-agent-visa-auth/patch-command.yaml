apiVersion: apps/v1
kind: Deployment
metadata:
  name: harmonia
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        args:
        - tximport
        - --agent
        - visa-auth
        - --no-user-input
        - --quiet
