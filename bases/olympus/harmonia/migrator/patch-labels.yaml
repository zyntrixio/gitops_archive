apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: harmonia
    component: migrator
  name: harmonia
  namespace: default
spec:
  selector:
    matchLabels:
      app: harmonia
      component: migrator
  template:
    metadata:
      labels:
        app: harmonia
        component: migrator
