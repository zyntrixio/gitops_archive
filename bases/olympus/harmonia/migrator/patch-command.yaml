apiVersion: apps/v1
kind: Deployment
metadata:
  name: harmonia
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        args:
        - bash
        - -c
        - alembic upgrade head; echo Done; tail -f /dev/null
