apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: harmonia
    component: matching-worker
  name: harmonia
  namespace: default
spec:
  selector:
    matchLabels:
      app: harmonia
      component: matching-worker
  template:
    metadata:
      labels:
        app: harmonia
        component: matching-worker
