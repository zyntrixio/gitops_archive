apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: harmonia
    component: export-retry-worker
  name: harmonia
  namespace: default
spec:
  selector:
    matchLabels:
      app: harmonia
      component: export-retry-worker
  template:
    metadata:
      labels:
        app: harmonia
        component: export-retry-worker
