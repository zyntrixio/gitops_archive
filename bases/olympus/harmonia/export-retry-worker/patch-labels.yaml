apiVersion: apps/v1
kind: Deployment
metadata:
  labels: &labels
    app: harmonia
    component: export-retry-worker
  name: harmonia
  namespace: default
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
