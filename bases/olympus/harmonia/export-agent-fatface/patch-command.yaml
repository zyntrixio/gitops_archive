apiVersion: apps/v1
kind: Deployment
metadata:
  name: harmonia
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        args:
        - txexport
        - --agent
        - fatface
        - --no-user-input
        - --quiet
