---
bases:
  - ../core

commonLabels:
  component: import-agent-harvey-nichols

nameSuffix: -import-agent-harvey-nichols

patches:
  - target:
      kind: Deployment
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: harmonia
        namespace: default
      spec:
        template:
          metadata:
            annotations:
              prometheus.io/port: '9100'
              prometheus.io/scrape: 'true'
              prometheus.io/job: harmonia-import-agent-harvey-nichols
          spec:
            containers:
              - name: app
                args: ["tximport", "--agent", "harvey-nichols", "--no-user-input", "--quiet"]
            priorityClassName: bink-low-tasks
