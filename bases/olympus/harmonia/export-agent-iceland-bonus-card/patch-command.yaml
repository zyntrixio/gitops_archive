apiVersion: apps/v1
kind: Deployment
metadata:
  name: harmonia
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        args:
        - txexport
        - --agent
        - iceland-bonus-card
        - --no-user-input
        - --quiet
