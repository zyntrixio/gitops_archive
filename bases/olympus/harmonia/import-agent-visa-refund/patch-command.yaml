apiVersion: apps/v1
kind: Deployment
metadata:
  name: harmonia
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        args:
        - tximport
        - --agent
        - visa-refund
        - --no-user-input
        - --quiet
