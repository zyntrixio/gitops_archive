---
bases:
  - ../core

commonLabels:
  component: import-agent-wasabi-club

nameSuffix: -import-agent-wasabi-club

patches:
  - target:
      kind: Deployment
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: harmonia
        namespace: default
      spec:
        template:
          metadata:
            annotations:
              prometheus.io/port: '9100'
              prometheus.io/scrape: 'true'
              prometheus.io/job: harmonia-import-agent-wasabi-club
          spec:
            containers:
              - name: app
                args: ["tximport", "--agent", "wasabi-club", "--no-user-input", "--quiet"]
                resources:
                  limits:
                    memory: 1024Mi
                  requests:
                    memory: 1024Mi
            priorityClassName: bink-low-tasks
