apiVersion: apps/v1
kind: Deployment
metadata:
  name: harmonia
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        args:
        - --agent
        - wasabi-club
        - --no-user-input
        - --quiet
        command:
        - tximport
