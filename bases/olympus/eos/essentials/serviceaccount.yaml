---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: eos
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${eos_client_id}
  labels:
    azure.workload.identity/use: "true"
