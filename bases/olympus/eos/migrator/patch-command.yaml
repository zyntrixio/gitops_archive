apiVersion: apps/v1
kind: Deployment
metadata:
  name: eos
spec:
  template:
    spec:
      containers:
      - name: app
        command: [ "linkerd-await", "--" ]
        args: [ "bash", "-c", "python manage.py migrate; echo Done; tail -f /dev/null" ]
