apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: eos
    component: migrator
  name: eos
  namespace: default
spec:
  selector:
    matchLabels:
      app: eos
      component: migrator
  template:
    metadata:
      labels:
        app: eos
        component: migrator
