apiVersion: apps/v1
kind: Deployment
metadata:
  name: eos
spec:
  template:
    spec:
      containers:
      - name: app
        command: [ "linkerd-await", "--" ]
        args: [ "python", "manage.py", "worker" ]
