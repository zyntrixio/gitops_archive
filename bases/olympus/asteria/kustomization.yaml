resources:
- deployment.yaml

components:
- ../../common/pgbouncer

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /metadata/annotations/buoyant.cloud~1excluded
        value: "true"
