---
resources:
  - ../core
  - service.yaml

commonLabels:
  component: api

nameSuffix: -api

patches:
  - target:
      kind: Deployment
    patch: |
      - op: add
        path: /spec/template/spec/priorityClassName
        value: bink-low-api
