---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: boreas
  name: boreas
  namespace: default
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 9000
  selector:
    app: boreas
  sessionAffinity: None
  type: ClusterIP
