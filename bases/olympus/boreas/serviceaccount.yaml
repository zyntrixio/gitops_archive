---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: boreas
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${boreas_client_id}
  labels:
    azure.workload.identity/use: "true"
