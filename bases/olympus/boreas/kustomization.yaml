---
resources:
  - deployment.yaml
  - service.yaml
  - serviceaccount.yaml

patches:
  - target:
      kind: Deployment
    patch: |
      - op: add
        path: /spec/template/spec/priorityClassName
        value: bink-low-api
