apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: postgres-exporter
    instance: hermes
  name: postgres-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: postgres-exporter
      instance: hermes
  template:
    metadata:
      labels:
        app: postgres-exporter
        instance: hermes
