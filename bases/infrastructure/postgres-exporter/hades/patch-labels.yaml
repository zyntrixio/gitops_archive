apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: postgres-exporter
    instance: hades
  name: postgres-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: postgres-exporter
      instance: hades
  template:
    metadata:
      labels:
        app: postgres-exporter
        instance: hades
