apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: postgres-exporter
    instance: harmonia
  name: postgres-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: postgres-exporter
      instance: harmonia
  template:
    metadata:
      labels:
        app: postgres-exporter
        instance: harmonia
