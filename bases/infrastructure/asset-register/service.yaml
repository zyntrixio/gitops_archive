---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: asset-register
  name: asset-register
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    app: asset-register
  sessionAffinity: None
  type: ClusterIP
