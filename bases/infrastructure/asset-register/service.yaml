---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: asset-register
    component: api
  name: asset-register
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    app: asset-register
    component: api
  sessionAffinity: None
  type: ClusterIP
