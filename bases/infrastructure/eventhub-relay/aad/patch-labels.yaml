apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    component: azureactivedirectory
  name: eventhub
  namespace: default
spec:
  selector:
    matchLabels:
      component: azureactivedirectory
  template:
    metadata:
      labels:
        component: azureactivedirectory
