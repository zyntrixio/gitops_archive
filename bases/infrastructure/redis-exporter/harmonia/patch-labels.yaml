apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: redis-exporter
    instance: harmonia
  name: redis-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: redis-exporter
      instance: harmonia
  template:
    metadata:
      labels:
        app: redis-exporter
        instance: harmonia
