apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-exporter
  namespace: monitoring
  labels: &labels
    app: redis-exporter
spec:
  selector:
    matchLabels: *labels
  template:
    metadata:
      annotations:
        prometheus.io/port: '9100'
        prometheus.io/scrape: 'true'
        prometheus.io/job: redis
      labels: *labels
    spec:
      containers:
      - name: redis-exporter
        image: binkext.azurecr.io/redis_exporter:v1.13.1
        ports:
        - containerPort: 9100
        env:
        - name: REDIS_ADDR
          valueFrom:
            secretKeyRef:
              key: uri_ssl
              name: azure-redis-common
        - name: REDIS_EXPORTER_PING_ON_CONNECT
          value: "true"
        - name: REDIS_EXPORTER_WEB_LISTEN_ADDRESS
          value: 0.0.0.0:9100
        resources:
          limits:
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 128Mi
      imagePullSecrets:
      - name: binkext.azurecr.io
