apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: redis-exporter
    instance: common
  name: redis-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: redis-exporter
      instance: common
  template:
    metadata:
      labels:
        app: redis-exporter
        instance: common
