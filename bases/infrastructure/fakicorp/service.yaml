apiVersion: v1
kind: Service
metadata:
  labels: &labels
    app: fakicorp
  name: fakicorp
  namespace: default
spec:
  ports:
  - name: http
    port: 8200
    protocol: TCP
  selector: *labels
  sessionAffinity: None
  type: ClusterIP
