apiVersion: v1
kind: Service
metadata:
  labels:
    app: fakicorp
  name: fakicorp
  namespace: default
spec:
  ports:
  - name: http
    port: 8200
    protocol: TCP
  selector:
    app: fakicorp
  sessionAffinity: None
  type: ClusterIP
