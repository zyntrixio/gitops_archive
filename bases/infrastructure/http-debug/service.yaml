---
apiVersion: v1
kind: Service
metadata:
  name: http-debug
  namespace: default
  labels:
    app: http-debug
spec:
  selector:
    app: http-debug
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
