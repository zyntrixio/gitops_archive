apiVersion: v1
kind: Service
metadata:
  labels: &labels
    app: simplenginx
  name: simplenginx
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector: *labels
  sessionAffinity: None
  type: ClusterIP
