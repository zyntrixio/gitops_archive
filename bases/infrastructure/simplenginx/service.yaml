apiVersion: v1
kind: Service
metadata:
  labels:
    app: simplenginx
  name: simplenginx
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: simplenginx
  sessionAffinity: None
  type: ClusterIP
