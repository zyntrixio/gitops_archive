---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: copybot
  name: copybot
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: copybot
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
        kubectl.kubernetes.io/default-container: app
      labels:
        app: copybot
    spec:
      priorityClassName: bink-high-tasks
      containers:
        - name: app
          image: binkcore.azurecr.io/events-copybot:2022-05-17T163804Z
          args: [tail, -f, /dev/null]
          resources:
            requests:
              cpu: 50m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 512Mi
      terminationGracePeriodSeconds: 30
      serviceAccountName: copybot
      imagePullSecrets:
        - name: binkcore.azurecr.io
