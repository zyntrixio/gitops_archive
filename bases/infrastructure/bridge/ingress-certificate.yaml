---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bridge.uksouth.bink.sh
  namespace: default
spec:
  dnsNames:
  - bridge.uksouth.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: bridge.uksouth.bink.sh
