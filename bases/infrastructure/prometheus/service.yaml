---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: prometheus
  labels:
    app: prometheus
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
    - appProtocol: http
      name: prometheus
      port: 9090
      protocol: TCP
      targetPort: prometheus
