apiVersion: v1
kind: Service
metadata:
  labels:
    app: binklink
  name: binklink
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: binklink
  sessionAffinity: None
  type: ClusterIP
