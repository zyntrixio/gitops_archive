apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: grafana
  namespace: flux-system
spec:
  interval: 60m
  url: https://grafana.github.io/helm-charts
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: grafana
  namespace: mimir
spec:
  chart:
    spec:
      chart: grafana
      version: "6.30.2"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
  interval: 60m
  values:
    # ingress:
    #   enabled: true
    #   annotations:
    #     kubernetes.io/ingress.class: nginx
    #     nginx.ingress.kubernetes.io/service-upstream: 'true'
    #   hosts:
    #     - host: grafana.${cluster_name}.${location}.bink.sh
    #       paths:
    #         - path: /
    #           pathType: Prefix
    #     - host: grafana.${cluster_name}.${location}.bink.host
    #       paths:
    #         - path: /
    #           pathType: Prefix
    #   tls:
    #     - secretName: grafana.${cluster_name}.${location}.bink.sh
    #       hosts:
    #         - grafana.${cluster_name}.${location}.bink.sh
    #     - secretName: grafana.${cluster_name}.${location}.bink.host
    #       hosts:
    #         - grafana.${cluster_name}.${location}.bink.host
    datasources:
      datasources.yaml:
        apiVersion: 1
        datasources:
        - name: Mimir
          type: prometheus
          url: http://mimir-query-frontend.mimir.svc:8080/prometheus
          access: proxy
          isDefault: true
          editable: true
