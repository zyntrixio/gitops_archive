---
bases:
  - github.com/vectordotdev/vector/distribution/kubernetes/vector-agent?ref=v0.22.1

resources:
  - namespace.yaml

namespace: vector

images:
  - name: timberio/vector
    newTag: 0.22.1-distroless-static

patches:
  - target:
      kind: DaemonSet
    patch: |-
      - op: add
        path: /metadata/annotations/buoyant.cloud~1excluded
        value: "true"
