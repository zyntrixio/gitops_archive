---
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - deployment.yaml
  - pvc.yaml

namespace: prometheus

configMapGenerator:
  - name: prometheus
    files:
      - config/prometheus.yaml
