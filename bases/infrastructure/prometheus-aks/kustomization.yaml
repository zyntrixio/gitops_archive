---
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  - service-lb.yaml

namespace: prometheus

configMapGenerator:
  - name: prometheus
    files:
      - prometheus/config.yaml
  - name: alertmanager
    files:
      - alertmanager/config.yaml
  - name: prometheus-msteams
    files:
      - prometheus-msteams/config.yaml
  - name: ssl-exporter
    files:
      - ssl-exporter/config.yaml
