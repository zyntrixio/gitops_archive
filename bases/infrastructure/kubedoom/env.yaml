
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubedoom
  namespace: kubedoom
spec:
  template:
    spec:
      containers:
      - name: kubedoom
        env:
        - name: NAMESPACE
          value: monitoring
