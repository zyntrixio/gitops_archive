---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: data-dashboard
  name: data-dashboard
  namespace: default
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: data-dashboard
  sessionAffinity: None
  type: ClusterIP
