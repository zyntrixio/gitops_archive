apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: carina
    component: migrator
  name: carina
  namespace: default
spec:
  selector:
    matchLabels:
      app: carina
      component: migrator
  template:
    metadata:
      labels:
        app: carina
        component: migrator
