apiVersion: apps/v1
kind: Deployment
metadata:
  name: carina
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        command:
        - bash
        - -c
        - linkerd-await; alembic upgrade head; echo Done; tail -f /dev/null
