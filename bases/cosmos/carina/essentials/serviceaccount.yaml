---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: carina
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${carina_client_id}
  labels:
    azure.workload.identity/use: "true"
