---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hubble
  namespace: default
annotations:
  azure.workload.identity/client-id: ${hubble_client_id}
labels:
    azure.workload.identity/use: "true"
