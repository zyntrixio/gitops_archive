apiVersion: apps/v1
kind: Deployment
metadata:
  name: vela
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        command: ["linkerd-await", "--"]
        args: ["python", "-m", "app.tasks.worker", "worker"]
