apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: vela
    component: task-worker
  name: vela
  namespace: default
spec:
  selector:
    matchLabels:
      app: vela
      component: task-worker
  template:
    metadata:
      labels:
        app: vela
        component: task-worker
        aadpodidbinding: vela
