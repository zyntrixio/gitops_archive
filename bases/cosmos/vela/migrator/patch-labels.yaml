apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: vela
    component: migrator
  name: vela
  namespace: default
spec:
  selector:
    matchLabels:
      app: vela
      component: migrator
  template:
    metadata:
      labels:
        app: vela
        component: migrator
