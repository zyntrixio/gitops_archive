apiVersion: apps/v1
kind: Deployment
metadata:
  name: vela
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: app
        command:
        - bash
        - -c
        - sleep 10; alembic upgrade head; echo Done; tail -f /dev/null
