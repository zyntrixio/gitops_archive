---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vela
  namespace: default
  annotations:
    azure.workload.identity/client-id: ${vela_client_id}
  labels:
    azure.workload.identity/use: "true"
