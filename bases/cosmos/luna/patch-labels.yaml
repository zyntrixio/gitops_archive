apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: luna
    component: api
  name: luna
  namespace: default
spec:
  selector:
    matchLabels:
      app: luna
      component: api
  template:
    metadata:
      labels:
        app: luna
        component: api
