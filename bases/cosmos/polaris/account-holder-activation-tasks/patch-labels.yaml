apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: polaris
    component: account-holder-activation-tasks
  name: polaris
  namespace: default
spec:
  selector:
    matchLabels:
      app: polaris
      component: account-holder-activation-tasks
  template:
    metadata:
      labels:
        app: polaris
        component: account-holder-activation-tasks
        aadpodidbinding: polaris
