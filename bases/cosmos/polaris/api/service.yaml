apiVersion: v1
kind: Service
metadata:
  labels:
    app: polaris
    component: api
  name: polaris-api
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    app: polaris
    component: api
  sessionAffinity: None
  type: ClusterIP
