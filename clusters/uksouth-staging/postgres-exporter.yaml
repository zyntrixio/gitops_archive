apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: postgres-exporter
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-staging/postgres-exporter
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
