apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: aperture
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-staging/portal/aperture
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  images:
    - name: binkcore.azurecr.io/aperture
      newTag: staging-2.1.54 # {"$imagepolicy": "flux-system:aperture-staging:tag"}
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars
