---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: mobsf
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-tools/mobsf
  dependsOn:
    - name: keyvault2kube
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    substituteFrom:
      - kind: Secret
        name: flux-cluster-vars
