apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: postgres-exporter-fs
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-sandbox/monitoring/postgres-exporter-fs
  prune: true
  force: true
  sourceRef:
    kind: GitRepository
    name: flux-system
