apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: keyvault-infra-sync
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-perf/keyvault-infra-sync
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
