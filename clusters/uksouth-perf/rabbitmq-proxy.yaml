apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: rabbitmq-proxy
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-perf/rabbitmq-proxy
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
