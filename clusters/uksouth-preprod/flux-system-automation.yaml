apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImageUpdateAutomation
metadata:
  name: flux-system
  namespace: flux-system
spec:
  checkout:
    branch: master
    gitRepositoryRef:
      name: flux-system
  commit:
    authorEmail: support@weave.works
    authorName: Flux Weave
    messageTemplate: '[ci skip] update image'
  interval: 1m0s
  update:
    strategy: Setters
