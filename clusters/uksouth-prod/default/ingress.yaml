apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: ingress
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-prod/default/ingress
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
