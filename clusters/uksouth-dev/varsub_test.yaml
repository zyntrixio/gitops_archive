apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: varsub-test
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-dev/varsub_test
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: cluster-vars
