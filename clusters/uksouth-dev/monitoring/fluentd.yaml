apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: fluentd
  namespace: flux-system
spec:
  dependsOn:
    - name: monitoring-namespace
  interval: 60m0s
  path: ./overlays/uksouth-dev/monitoring/fluentd
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
