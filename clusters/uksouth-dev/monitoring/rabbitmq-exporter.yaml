apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: rabbitmq-exporter
  namespace: flux-system
spec:
  dependsOn:
    - name: monitoring-namespace
  interval: 60m0s
  path: ./overlays/uksouth-dev/rabbitmq-exporter
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
