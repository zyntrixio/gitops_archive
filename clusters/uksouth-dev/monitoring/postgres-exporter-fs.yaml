apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: postgres-exporter-fs
  namespace: flux-system
spec:
  dependsOn:
    - name: monitoring-namespace
  interval: 60m0s
  path: ./overlays/uksouth-dev/monitoring/postgres-exporter-fs
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
