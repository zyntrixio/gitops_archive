---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: data-ingress
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-dev/data/ingress
  prune: true
  dependsOn:
    - name: cert-manager
    - name: keyvault2kube
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars
