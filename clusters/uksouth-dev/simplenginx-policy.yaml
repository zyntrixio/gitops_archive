apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImagePolicy
metadata:
  name: simplenginx
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: simplenginx
  filterTags:
    pattern: '^(?P<ts>[0-9-\.]+)'
    extract: '$ts'
  policy:
    numerical:
      order: asc
