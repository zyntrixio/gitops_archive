apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImagePolicy
metadata:
  name: simplenginx
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: simplenginx
  policy:
    numerical:
      order: asc
