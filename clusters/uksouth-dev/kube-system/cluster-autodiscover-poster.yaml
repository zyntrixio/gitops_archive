apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: cluster-autodiscover-poster
  namespace: flux-system
spec:
  interval: 60m0s
  path: ./overlays/uksouth-dev/cluster-autodiscover-poster
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: cluster-vars
