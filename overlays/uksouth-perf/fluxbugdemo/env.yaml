apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluxbugdemo
spec:
  template:
    spec:
      containers:
      - name: app
        env:
          - name: foo
            value: bar
          - name: jar
            value: car
