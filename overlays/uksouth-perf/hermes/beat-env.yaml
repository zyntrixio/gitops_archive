apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-beat
spec:
  template:
    spec:
      containers:
      - name: app
        env:
        - name: SENTRY_SAMPLE_RATE
          value: '0.01'
