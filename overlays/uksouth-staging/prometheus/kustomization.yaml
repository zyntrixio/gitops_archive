bases:
  - ../../../bases/infrastructure/prometheus-aks

configMapGenerator:
  - name: alertmanager_rules
    files:
      - config/rules.yaml

patches:
  - target:
      kind: Deployment
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: prometheus
      spec:
        template:
          spec:
            containers:
              - name: prometheus
                volumeMounts:
                  - name: alertmanager_rules
                    mountPath: /etc/prometheus/rules.yaml
                    subPath: rules.yaml
            volumes:
              - configMap:
                  defaultMode: 420
                  name: alertmanager_rules
                name: alertmanager_rules
