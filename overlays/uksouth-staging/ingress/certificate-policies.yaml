apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: policies.staging0.uksouth.bink.sh
  namespace: default
spec:
  dnsNames:
  - policies.staging0.uksouth.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: policies.staging0.uksouth.bink.sh
