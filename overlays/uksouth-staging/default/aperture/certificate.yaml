---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: aperture.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
    - aperture.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: aperture.${cluster_name}.${location}.bink.sh
