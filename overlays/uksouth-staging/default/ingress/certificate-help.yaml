---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: help.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
    - help.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: help.${cluster_name}.${location}.bink.sh
