---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: help.${cluster_name}.${location}.bink.sh
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/service-upstream: 'true'
spec:
  rules:
    - host: help.${cluster_name}.${location}.bink.sh
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: help
                port:
                  number: 80
  tls:
    - hosts:
        - help.${cluster_name}.${location}.bink.sh
