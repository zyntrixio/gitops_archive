bases:
  - ../../../../../bases/devops/snowstorm/essentials

resources:
  - aad.yaml

patches:
  - target:
      kind: Job
      name: snowstorm-migrator
    patch: |
      apiVersion: batch/v1
      kind: Job
      metadata:
        name: snowstorm-migrator
      spec:
        template:
          spec:
            containers:
            - name: app
              env:
                - name: database_dsn
                  valueFrom:
                    secretKeyRef:
                      key: url_snowstorm
                      name: azure-postgres
