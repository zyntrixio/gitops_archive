apiVersion: apps/v1
kind: Deployment
metadata:
  name: midas
spec:
  template:
    spec:
      containers:
      - name: app
        args:
        - celery
        - -A
        - app.api.celery
        - beat
        - --schedule
        - /tmp/beat
        - --pidfile
        - /tmp/beat.pid
