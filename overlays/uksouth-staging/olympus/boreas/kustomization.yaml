---
resources:
  - ../../../../bases/olympus/boreas
  # - ingress.yaml

namespace: olympus

patches:
  - target:
      kind: Deployment
    patch: |
      - op: add
        path: /spec/template/spec/containers/0/env/-
        value:
          name: SENTRY_ENVIRONMENT
          value: staging
