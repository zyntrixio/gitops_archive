---
bases:
  - ../../../../bases/qa/pyqa-bpl-auto

resources:
  - aad.yaml

namespace: bpl-testing

images:
  - name: binkcore.azurecr.io/pyqa-bpl-auto
    newName: binkcore.azurecr.io/pyqa-bpl-auto

configMapGenerator:
  - name: pyqa-bpl-auto
    envs:
      - env.ini

patches:
  - target:
      kind: Deployment
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: pyqa-bpl-auto
      spec:
        template:
          spec:
            containers:
              - name: app
                env:
                  - name: BLOB_STORAGE_DSN
                    valueFrom:
                      secretKeyRef:
                        key: connection_string
                        name: azure-storage-common
                envFrom:
                  - configMapRef:
                      name: pyqa-bpl-auto
