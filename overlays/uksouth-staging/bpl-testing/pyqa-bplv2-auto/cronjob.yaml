---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pyqa-bplv2
  namespace: default
spec:
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 1
  successfulJobsHistoryLimit: 1
  schedule: 00 01 * * *
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        metadata:
          annotations:
            linkerd.io/inject: enabled
            config.linkerd.io/shutdown-grace-period: "30s"
            kubectl.kubernetes.io/default-container: app
          labels:
            app: pyqa-bplv2
            aadpodidbinding: pyqa-bplv2
        spec:
          restartPolicy: Never
          imagePullSecrets:
            - name: binkcore.azurecr.io
          volumes:
            - name: data
              emptyDir: {}
          containers:
            - name: script
              command: [linkerd-await, --shutdown, --]
              args:
                - bash
                - -c
                - ./report-render.sh
              image: binkcore.azurecr.io/pyqa-bplv2:latest
              securityContext:
                runAsGroup: 0
                runAsUser: 0
              imagePullPolicy: Always
              volumeMounts:
                - mountPath: /tmp
                  name: data
            - name: app
              command: [linkerd-await, --]
              args: ["bash", "-c", "pytest --html /tmp/report.html --self-contained-html -m bpl && echo $? > /tmp/status.txt || true"]
              image: binkcore.azurecr.io/pyqa-bplv2:latest
              volumeMounts:
                - mountPath: /tmp
                  name: data
              securityContext:
                runAsGroup: 0
                runAsUser: 0
              imagePullPolicy: Always
              resources:
                limits:
                  memory: 500Mi
                requests:
                  cpu: 10m
                  memory: 500Mi
