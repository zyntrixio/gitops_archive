---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: barclays-sit-reflector.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
    - barclays-sit-reflector.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: barclays-sit-reflector.${cluster_name}.${location}.bink.sh
