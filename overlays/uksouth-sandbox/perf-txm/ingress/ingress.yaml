---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/service-upstream: 'true'
    nginx.ingress.kubernetes.io/use-regex: 'true'
    nginx.ingress.kubernetes.io/enable-access-log: 'false'
  name: perf-txm.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  rules:
    - host: perf-txm.${cluster_name}.${location}.bink.sh
      http:
        paths:
          - path: /healthz
            pathType: Exact
            backend:
              service:
                name: healthz
                port:
                  number: 80
  tls:
    - hosts:
        - perf-txm.${cluster_name}.${location}.bink.sh
