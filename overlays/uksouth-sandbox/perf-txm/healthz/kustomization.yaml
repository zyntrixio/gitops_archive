---
bases:
  - ../../../../bases/infrastructure/healthz

namespace: perf-txm

patches:
  - target:
      kind: ServiceProfile
    patch: |-
      - op: replace
        path: /metadata/name
        value: healthz.perf-txm.svc.cluster.local
