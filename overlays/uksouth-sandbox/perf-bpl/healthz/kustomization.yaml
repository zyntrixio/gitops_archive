---
bases:
  - ../../../../bases/infrastructure/healthz

namespace: perf-bpl

patches:
  - target:
      kind: ServiceProfile
    patch: |-
      - op: replace
        path: /metadata/name
        value: healthz.perf-bpl.svc.cluster.local
