---
bases:
  - ../../../../bases/olympus/api2-docs
  - ../../../../bases/common/issuer

resources:
  - certificate.yaml
  - ingress.yaml
  - ingress-healthz.yaml

namespace: default

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/env
        value:
          - name: DEPLOYED_ENV
            value: sandbox


# huge-guilt-meshwork-pizza
secretGenerator:
  - name: api2-docs-basicauth
    files:
      - auth
