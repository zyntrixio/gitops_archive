apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sit-barclays.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
  - sit-barclays.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: sit-barclays.${cluster_name}.${location}.bink.sh
