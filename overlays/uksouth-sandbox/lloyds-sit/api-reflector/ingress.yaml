---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/service-upstream: 'true'
    nginx.ingress.kubernetes.io/upstream-vhost: lloyds-sit-reflector.sandbox.gb.bink.com
  name: lloyds-sit-reflector.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  rules:
    - host: lloyds-sit-reflector.${cluster_name}.${location}.bink.sh
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-reflector
                port:
                  number: 80
  tls:
    - hosts:
        - lloyds-sit-reflector.${cluster_name}.${location}.bink.sh
