---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lloyds-sit-reflector.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
    - lloyds-sit-reflector.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: lloyds-sit-reflector.${cluster_name}.${location}.bink.sh
