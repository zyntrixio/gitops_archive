---
bases:
  - ../../../../bases/infrastructure/healthz

namespace: perf-api-v2

patches:
  - target:
      kind: ServiceProfile
    patch: |-
      - op: replace
        path: /metadata/name
        value: healthz.perf-api-v2.svc.cluster.local
