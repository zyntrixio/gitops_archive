apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sit.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
  - sit.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: sit.${cluster_name}.${location}.bink.sh
