apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oat.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
  - oat.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: oat.${cluster_name}.${location}.bink.sh
