---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: barclays-oat.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
    - barclays-oat.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: barclays-oat.${cluster_name}.${location}.bink.sh
