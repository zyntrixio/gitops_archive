---
bases:
  - ../../../../bases/infrastructure/healthz

namespace: barclays-oat

patches:
  - target:
      kind: ServiceProfile
    patch: |-
      - op: replace
        path: /metadata/name
        value: healthz.barclays-oat.svc.cluster.local
