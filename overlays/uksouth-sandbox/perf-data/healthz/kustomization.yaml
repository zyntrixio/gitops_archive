---
bases:
  - ../../../../bases/infrastructure/healthz

namespace: perf-data

patches:
  - target:
      kind: ServiceProfile
    patch: |-
      - op: replace
        path: /metadata/name
        value: healthz.perf-data.svc.cluster.local
