apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sit-lbg.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
  - sit-lbg.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: sit-lbg.${cluster_name}.${location}.bink.sh
