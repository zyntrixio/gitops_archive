apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api.preprod0.uksouth.bink.sh
  namespace: default
spec:
  dnsNames:
  - api.preprod0.uksouth.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: api.preprod0.uksouth.bink.sh
