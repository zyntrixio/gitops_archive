resources:
  - certs.yaml
  - deploy.yaml
  - ingress.yaml
  - namespace.yaml
  - pvc.yaml
  - service.yaml

namespace: grafana

configMapGenerator:
  - name: grafana
    files:
      - config/grafana.ini
