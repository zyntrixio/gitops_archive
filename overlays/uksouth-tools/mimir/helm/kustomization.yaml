resources:
  - namespace.yaml
  - helm.yaml

configMapGenerator:
  - files:
      - config/mimir.yaml
      - config/alertmanager.yaml
    name: mimir-external-config
    options:
      disableNameSuffixHash: true

namespace: mimir
