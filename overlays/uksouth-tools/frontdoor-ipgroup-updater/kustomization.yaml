---
namespace: default

secretGenerator:
  - name: frontdoor-ipgroup-updater
    files:
      - secret/application_secret

configMapGenerator:
  - name: frontdoor-ipgroup-updater
    files:
      - config/config.toml

resources:
  - cronjob.yaml
