apiVersion: apps/v1
kind: Deployment
metadata:
  name: keyvault-infra-sync
  namespace: kube-system
spec:
  template:
    spec:
      containers:
      - name: app
        env:
        - name: KEY_VAULT_URLS
          value: https://bink-uksouth-tools-inf.vault.azure.net/
