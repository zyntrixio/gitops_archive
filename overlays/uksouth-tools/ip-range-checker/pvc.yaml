---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ip-range-checker-elasticsearch-data
  labels:
    app: ip-range-checker
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
