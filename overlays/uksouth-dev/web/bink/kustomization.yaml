---
bases:
  - ../../../../bases/web/bink

namespace: web

configMapGenerator:
  - name: web-bink
    envs:
      - env.ini

patches:
  - target:
      kind: Deployment
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: web-bink
      spec:
        template:
          spec:
            containers:
              - name: app
                envFrom:
                  - configMapRef:
                      name: web-bink
