---
bases:
  - ../../../bases/infrastructure/vector

namespace: vector

configMapGenerator:
  - name: vector-agent-config
    files:
      - vector.toml

secretGenerator:
  - name: vector-opensearch
    files:
      - opensearch-ca.pem

patches:
  - path: volumes.yaml
    target:
      kind: DaemonSet
