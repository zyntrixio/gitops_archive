apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: postgres-exporter
    instance: common
  name: postgres-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: postgres-exporter
      instance: common
  template:
    metadata:
      labels:
        app: postgres-exporter
        instance: common
