apiVersion: apps/v1
kind: Deployment
metadata:
  name: varsub-test
  labels:
    app: varsub-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: varsub-test
  template:
    metadata:
      labels:
        app: varsub-test
    spec:
      containers:
      - name: app
        image: nginx:alpine
        ports:
        - containerPort: 80
        env:
        - name: jeff
          value: ${jeff}
