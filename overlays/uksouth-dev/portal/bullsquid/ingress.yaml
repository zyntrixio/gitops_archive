---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: portal.${cluster_name}.${cluster_location}.bink.sh-bullsquid
  namespace: default
spec:
  rules:
    - host: portal.${cluster_name}.${cluster_location}.bink.sh
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: bullsquid-api
                port:
                  number: 80
  tls:
    - hosts:
        - portal.${cluster_name}.${cluster_location}.bink.sh
