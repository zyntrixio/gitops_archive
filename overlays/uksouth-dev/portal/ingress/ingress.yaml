---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/service-upstream: 'true'
  name: portal.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  rules:
    - host: portal.${cluster_name}.${location}.bink.sh
      http:
        paths:
          - path: /api/v1/plans
            pathType: Prefix
            backend:
              service:
                name: bullsquid-api
                port:
                  number: 80
          - path: /healthz
            pathType: Prefix
            backend:
              service:
                name: healthz
                port:
                  number: 80
  tls:
    - hosts:
        - portal.${cluster_name}.${location}.bink.sh
      secretName: portal.${cluster_name}.${location}.bink.sh
