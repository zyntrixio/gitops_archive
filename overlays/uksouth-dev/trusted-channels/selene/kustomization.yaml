---
bases:
  - ../../../../bases/olympus/selene

namespace: trusted-channels

configMapGenerator:
  - name: selene
    envs:
      - env.ini

patches:
  - target:
      kind: Deployment
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: selene
      spec:
        template:
          spec:
            containers:
              - name: app
                envFrom:
                  - configMapRef:
                      name: selene
