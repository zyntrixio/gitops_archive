apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-reflector
spec:
  template:
    spec:
      containers:
      - name: app
        env:
        - name: log_json
          value: "true"
        - name: postgres_dsn
          value: postgresql://common@bink-uksouth-dev-common:@127.0.0.1:5432/api_reflector
        - name: secret_key
          value: i9B1IvrCvm3tcQBwt7jDkoRpvtJRgbcu
          # generated with: LC_ALL=C tr -dc A-Za-z0-9 </dev/urandom | head -c 32
          # Not a real secret value, just something Flask uses
