---
bases:
  - ../../../../bases/olympus/asteria

configMapGenerator:
  - name: asteria
    namespace: default
    envs:
      - env.ini

patches:
  - target:
      kind: Deployment
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: asteria
      spec:
        replicas: 0
        template:
          spec:
            containers:
              - name: app
                envFrom:
                  - configMapRef:
                      name: asteria
                env:
                  - name: POSTGRES_DSN
                    valueFrom:
                      secretKeyRef:
                        name: azure-pgfs
                        key: common_hermes
