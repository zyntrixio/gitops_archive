apiVersion: apps/v1
kind: Deployment
metadata:
  name: envs
spec:
  template:
    spec:
      containers:
      - name: app
        env:
        - name: DATABASE_USER
          valueFrom:
            secretKeyRef:
              key: admin_user
              name: azure-pg-common
