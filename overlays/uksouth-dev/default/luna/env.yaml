apiVersion: apps/v1
kind: Deployment
metadata:
  name: luna
spec:
  template:
    spec:
      containers:
      - name: app
        env:
        - name: REDIS_URL
          value: redis://redis:6379/0
        - name: USE_REDIS_CACHE
          value: "true"
