---
resources:
  - ../../../../bases/cosmos/event-horizon
  - aad.yaml

namespace: bpl

replicas:
  - name: event-horizon
    count: 3

configMapGenerator:
  - name: event-horizon
    envs:
      - env.ini
  - name: eh-hacks
    files:
      - hacks/enqueue_tx_data.py
      - hacks/insert_tx_direct.py
      - hacks/prod-cleaned.json

patches:
  - target:
      kind: Deployment
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: event-horizon
      spec:
        template:
          spec:
            volumes:
              - name: hacks
                configMap:
                  name: eh-hacks
                  items:
                    - key: enqueue_tx_data.py
                      path: enqueue_tx_data.py
                    - key: insert_tx_direct.py
                      path: insert_tx_direct.py
                    - key: prod-cleaned.json
                      path: prod-cleaned.json
            containers:
              - name: app
                volumeMounts:
                  - name: hacks
                    mountPath: /app/hacks/
                envFrom:
                  - configMapRef:
                      name: event-horizon
                env:
                  - name: POLARIS_DATABASE_URI
                    valueFrom:
                      secretKeyRef:
                        name: azure-postgres
                        key: url_polaris
                  - name: VELA_DATABASE_URI
                    valueFrom:
                      secretKeyRef:
                        name: azure-postgres
                        key: url_vela
                  - name: CARINA_DATABASE_URI
                    valueFrom:
                      secretKeyRef:
                        name: azure-postgres
                        key: url_carina
                  - name: DATABASE_URI
                    valueFrom:
                      secretKeyRef:
                        name: azure-postgres
                        key: url_placeholder
