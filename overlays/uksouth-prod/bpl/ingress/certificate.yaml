---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bpl.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
    - bpl.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: bpl.${cluster_name}.${location}.bink.sh
