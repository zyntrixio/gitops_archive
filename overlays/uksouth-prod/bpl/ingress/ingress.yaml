---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/service-upstream: 'true'
    nginx.ingress.kubernetes.io/use-regex: "true"
  name: bpl.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  rules:
    - host: bpl.${cluster_name}.${location}.bink.sh
      http:
        paths:
          - path: /bpl/admin
            pathType: Prefix
            backend:
              service:
                name: event-horizon
                port:
                  number: 80
          - path: ^/bpl/loyalty/[a-z0-9-]{1,100}/marketing/unsubscribe?.*
            pathType: Exact
            backend:
              service:
                name: polaris-api
                port:
                  number: 80
          - path: /healthz
            pathType: Exact
            backend:
              service:
                name: healthz
                port:
                  number: 80
  tls:
    - hosts:
        - bpl.${cluster_name}.${location}.bink.sh
      secretName: bpl.${cluster_name}.${location}.bink.sh
