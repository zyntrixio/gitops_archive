---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/service-upstream: 'true'
  name: link.${cluster_name}.${cluster_location}.bink.sh
  namespace: default
spec:
  rules:
    - host: link.${cluster_name}.${cluster_location}.bink.sh
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: binklink
                port:
                  number: 80
  tls:
    - hosts:
        - link.${cluster_name}.${cluster_location}.bink.sh
