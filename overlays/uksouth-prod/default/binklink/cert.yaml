apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: link.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
  - link.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: link.${cluster_name}.${location}.bink.sh
