---
bases:
  - ../../../../bases/infrastructure/binklink

resources:
  - cert.yaml
  - ingress.yaml
  - ingress-healthz.yaml

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /spec/replicas
        value: 3
