---
bases:
  - ../../../../bases/olympus/asteria

replicas:
  - name: asteria
    count: 0

patches:
  - target:
      kind: Deployment
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: asteria
      spec:
        template:
          spec:
            containers:
              - name: app
                env:
                  - name: POSTGRES_DSN
                    valueFrom:
                      secretKeyRef:
                        key: common_hermes
                        name: azure-pgfs
