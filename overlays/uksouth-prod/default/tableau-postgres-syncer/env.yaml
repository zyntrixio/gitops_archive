apiVersion: apps/v1
kind: Deployment
metadata:
  name: tableu-postgres-syncer
spec:
  template:
    spec:
      containers:
      - name: syncer        
        env:
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              key: uri_ssl
              name: azure-redis-vnet
        - name: SOURCE_DB_HOST
          value: 127.0.0.1
        - name: SOURCE_DBS
          value: hermes|hermes@bink-uksouth-prod-herme
        - name: DEST_DB_HOST
          value: bink-tableau-uksouth.postgres.database.azure.com
        - name: DEST_DB_USER
          value: laadmin@bink-tableau-uksouth
        - name: DEST_DB_PASSWORD
          value: hbhboGJzguG1hebAkALxvAnL
