---
bases:
  - ../../../../bases/olympus/europa/api
  - ../../../../bases/olympus/europa/migrator

resources:
  - aad.yaml

patches:
  - path: env.yaml
    target:
      kind: Deployment
  - target:
      kind: Deployment
      name: europa-api
    patch: |-
      - op: add
        path: /spec/replicas
        value: 3
