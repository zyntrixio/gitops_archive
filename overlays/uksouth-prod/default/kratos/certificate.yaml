---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kratos.${cluster_name}.${location}.bink.sh
  namespace: default
spec:
  dnsNames:
    - kratos.${cluster_name}.${location}.bink.sh
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: letsencrypt
  secretName: kratos.${cluster_name}.${location}.bink.sh
