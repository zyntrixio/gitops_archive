---
resources:
  - ../../../../bases/olympus/zephyrus

namespace: default

configMapGenerator:
  - name: zephyrus
    envs:
      - env.ini

replicas:
  - name: zephyrus-api
    count: 3

patches:
  - target:
      kind: Deployment
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/envFrom
        value:
          - configMapRef:
              name: zephyrus
