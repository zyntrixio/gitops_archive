---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: help.${cluster_name}.${cluster_location}.bink.sh-healthz
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/service-upstream: 'true'
    nginx.ingress.kubernetes.io/enable-access-log: 'false'
spec:
  rules:
    - host: help.${cluster_name}.${cluster_location}.bink.sh
      http:
        paths:
          - path: /healthz
            pathType: Exact
            backend:
              service:
                name: healthz
                port:
                  number: 80
  tls:
    - hosts:
        - help.${cluster_name}.${cluster_location}.bink.sh
