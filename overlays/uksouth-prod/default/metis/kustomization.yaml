---
bases:
  - ../../../../bases/olympus/metis/api
  - ../../../../bases/olympus/metis/celery

resources:
  - aad.yaml

patches:
  - path: env.yaml
    target:
      kind: Deployment
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /spec/replicas
        value: 3
