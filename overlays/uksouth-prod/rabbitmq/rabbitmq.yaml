---
apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  name: rabbitmq
  namespace: rabbitmq
spec:
  replicas: 3
  resources:
    requests:
      cpu: 1000m
      memory: 16Gi
    limits:
      cpu: 4000m
      memory: 28Gi
  tolerations:
    - key: app
      operator: Equal
      value: rabbitmq
      effect: NoSchedule
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: agentpool
                operator: In
                values:
                  - rabbitmq
