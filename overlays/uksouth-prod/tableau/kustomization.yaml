resources:
  - ../../../bases/infrastructure/healthz # provides namespace
  - service.yaml
  - ingress-admin.yaml

namespace: tableau

patches:
  - target:
      kind: ServiceProfile
    patch: |
      - op: replace
        path: /metadata/name
        value: healthz.tableau.svc.cluster.local
