kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: grafana
  namespace: grafana
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  volumeName: grafana
  storageClassName: bink-azureblob-nfs
