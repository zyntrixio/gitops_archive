---
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

namespace: devops

replicas:
  - name: pypi
    count: 3

configMapGenerator:
  - name: pypi
    files:
      - config/config.ini
